export const metadata = {
  title: 'Sign In',
  description: 'Sign in to your MS Cinemas account to manage your bookings and movie tickets.',
};

export default function SignInLayout({ children }) {
  return children;
}

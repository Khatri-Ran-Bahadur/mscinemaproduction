export const metadata = {
  title: 'Movie Details',
  description: 'View showtimes, movie synopsis, cast details and book your tickets online for the latest movies at MS Cinemas.',
};

export default function MovieDetailLayout({ children }) {
  return children;
}
